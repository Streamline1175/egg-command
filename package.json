{
  "name": "egg-command",
  "version": "2.0.0",
  "description": "Local-first dashboard for Big Green Egg Genius and Flame Boss.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"node server.js\" \"cd client && npm run dev\"",
    "install-all": "npm install && cd client && npm install",
    "build": "cd client && npm run build",
    "test": "node --test test/*.test.js",
    "probe": "node scripts/probe.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mqtt": "^5.16.0"
  },
  "devDependencies": {
    "aedes": "^0.51.3",
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
